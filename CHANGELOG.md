# Changelog

## Unreleased

- Make `/commit` automatically include, safely stage, and commit all changes; `--all` remains a backwards-compatible alias.

## 0.1.0

- Initial GitHub-installable `/commit` extension.
